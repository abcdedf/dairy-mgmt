# Claude Code — Session Startup Instructions

## Copy and paste this at the start of every Claude Code session:

---

This is a Flutter + WordPress dairy production management system.

Read CLAUDE.md in this folder — it has the full project brief including architecture,
folder structure, DB schema, API endpoints, and coding conventions.

The two components are:
- dfm/              — Flutter mobile + web app (GetX, Dart)
-  dfm-backend/  — WordPress plugin code repository (deploy to your WP plugins directory)

Both components must stay in sync. When I ask for a change, read the relevant files
in both components before making any edits.

Always output complete files — I replace files wholesale, never apply partial patches.
Before outputting any file, diff it against the original to confirm only intended lines are changed and correct.

---

## Then describe your task, for example:

